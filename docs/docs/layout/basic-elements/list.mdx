---
sidebar_position: 1
---

import ExampleTabs from '@site/src/components/ExampleTabs'
import 'semantic-ui-css/semantic.min.css'

# List

A [Semantic-UI List](https://semantic-ui.com/elements/list.html#/definition) element
grouping its contents into a list of items.

```yaml
component: 'list'
```

:::note Implementation state:

- **Jinja2**: 🏗
- **React**: [✅](https://github.com/oarepo/react-generative-ui/blob/master/src/components/List.tsx)

:::

## Properties

| Name         | Description                                                  |
| ------------ | ------------------------------------------------------------ |
| `item`       | _CDB_ description of how individual items should be rendered |
| `items`      | list of items, either strings or objects with item props     |
| `horizontal` | list should be rendered horizontally                         |
| `*`          | any additional props passed to the wrapping list element     |

:::tip Additional Docs

- [Semantic-UI List](https://semantic-ui.com/elements/list.html#/definition)
- [Semantic-UI React List](https://react.semantic-ui.com/elements/list/)

:::

## Data

Uses any data referenced by the `dataField` as list `items` data:

```jsx
<List items={referencedData} ... />
```

## Example

### Simple list

<ExampleTabs
  layout={[
    {
      component: 'list',
      items: ['this is item #1', 'this is item #2', 'this is item #3'],
    },
  ]}
/>

### Horizontal data list

<ExampleTabs
  noResult
  data={{
    itemValues: [
      'this is data item #1',
      'this is data item #2',
      'this is data item #3',
    ],
  }}
  layout={[
    {
      component: 'list',
      horizontal: true,
      dataField: 'itemValues',
    },
  ]}
/>

### Custom item component list

<ExampleTabs
  layout={[
    {
      component: 'list',
      horizontal: true,
      items: [
        { content: 'this is data item #1', color: 'red' },
        { header: 'this is data item #2', color: 'green', size: 'large' },
        { content: 'this is data item #3' },
      ],
      item: {
        component: 'card',
        color: 'blue',
      },
    },
  ]}
/>
