---
sidebar_position: 1
---

import ExampleTabs from '@site/src/components/ExampleTabs'
import 'semantic-ui-css/semantic.min.css'

# Semantic-UI elements

A raw [Semantic-UI](https://semantic-ui.com/) elements can be used
given by its name. Any props from _CDB_ are passed directly to
the element.

If no Semantic-UI element could be found by the name, a plain HTML element is used.

```yaml
component: '<semantic-ui-element-name>'
```

:::note Implementation state:

- **Jinja2**: 🏗
- **React**: [✅](https://github.com/oarepo/react-generative-ui/blob/master/src/components/SemanticFallback.tsx)

:::

## Properties

| Name | Description                 |
| ---- | --------------------------- |
| `*`  | any supported element props |

:::tip Additional Docs

- [Semantic-UI](https://semantic-ui.com/)
- [Semantic-UI React](https://react.semantic-ui.com/)

:::

## Data

Renders any data referenced by the `data` path as:

```jsx
<semantic-ui-element-name {/*...*/}>{referencedData}</semantic-ui-element-name>
```

## Example

<ExampleTabs
  data={{
    test: {
      nested: 'Hello world',
    },
  }}
  layout={[
    {
      component: 'rating',
      icon: 'heart',
      defaultRating: 4,
      maxRating: 5,
    },
    {
      component: 'flag',
      name: 'cz',
    },
    {
      component: 'label',
      data: 'test.nested',
    },
  ]}
/>
