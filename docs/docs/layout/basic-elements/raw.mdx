---
sidebar_position: 1
---

import ExampleTabs from '@site/src/components/ExampleTabs'
import 'semantic-ui-css/semantic.min.css'

# Raw

A proxy element that just resolves and renders any data passed to it.

```yaml
component: 'raw'
```

:::note Implementation state:

- **Jinja2**: 🏗
- **React**: [✅](https://github.com/oarepo/react-generative-ui/blob/master/src/components/Raw.tsx)

:::

## Properties

<!-- TODO: describe any properties accepted by an element -->

| Name       | Description                  |
| ---------- | ---------------------------- |
| `children` | main contents of the element |

## Data

Renders any data referenced by the `data` path as-is:

```django
{referencedData}
```

## Example

### Raw children

<ExampleTabs
  layout={[
    {
      component: 'raw',
      children: 'Wow, it just prints this string',
    },
  ]}
/>

### Raw data

<ExampleTabs
  disableResult
  data={{
    rawText: 'Wow, it just prints this string from data context',
  }}
  layout={[
    {
      component: 'raw',
      dataField: 'rawText',
    },
  ]}
/>
