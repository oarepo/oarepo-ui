---
sidebar_position: 1
---

import ExampleTabs from '@site/src/components/ExampleTabs'
import 'semantic-ui-css/semantic.min.css'

# Row

A layout element that places its items in a grid row width
customizable columns.

```yaml
component: 'row'
```

:::note Implementation state:

- **Jinja2**: 🏗
- **React**: [✅](https://github.com/oarepo/react-generative-ui/blob/master/src/components/structural/Row.tsx)

:::

## Properties

| Name            | Description                                                                                                                                                 |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `columns`       | list of _CDB_ items to be rendered in a row. Assumes [Column](column) definitions by default, any other component is wrapped in a [Column](column) element. |
| `columnsPerRow` | (default: `equal`) number of columns rendered in the GridRow                                                                                                |
| `children`      | children elements passed directly to the Grid Row wrapping element                                                                                          |
| `*`             | any other properties passed to the Grid Row                                                                                                                 |

:::tip Additional Docs

- [Semantic-UI Grid Row](https://semantic-ui.com/collections/grid.html#grouping)
- [Semantic-UI React Grid.Row](https://react.semantic-ui.com/collections/grid/)

:::

## Data

<!-- TODO: does element render any data given by `data` path? -->

:::info Data rendering

Doesn't directly render any data referenced by the `dataField`.

:::

## Example

### Simple row items

<ExampleTabs
  noResult
  layout={[
    {
      component: 'row',
      items: [
        { component: 'strong', children: 'this is item #1' },
        { component: 'strong', children: 'this is item #2' },
        { component: 'strong', children: 'this is item #3' },
      ],
    },
  ]}
/>

### String-separated items

<ExampleTabs
  noResult
  layout={[
    {
      component: 'row',
      separator: ' || ',
      items: [
        { component: 'strong', children: 'this is item #1' },
        { component: 'strong', children: 'this is item #2' },
        { component: 'strong', children: 'this is item #3' },
      ],
    },
  ]}
/>

### Component-separated items

<ExampleTabs
  noResult
  layout={[
    {
      component: 'row',
      separator: {
        component: 'img',
        src: '/oarepo-ui/img/logo.png',
        width: 20,
        height: 20,
      },
      items: [
        { component: 'span', children: 'this is item #1' },
        { component: 'span', children: 'this is item #2' },
        { component: 'span', children: 'this is item #3' },
      ],
    },
  ]}
/>
