---
sidebar_position: 1
---

import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import { GeneratedUI } from 'react-generative-ui'

# Grid

A [Semantic-UI Grid](https://semantic-ui.com/collections/grid.html#/definition) container element that
renders its contents in a responsive columns and/or rows.

```yaml
component: 'grid'
```

:::note Implementation state:

- **Jinja2**: [🏗](https://github.com/oarepo/oarepo-ui/blob/invenio-3.5-jinja/oarepo_ui/templates/oarepo_ui/components/components.html.jinja2#L2)
- **React**: [✅](https://github.com/oarepo/react-generative-ui/blob/master/src/components/Grid.tsx)

:::

## Properties

| Name            | Description                                       |
| --------------- | ------------------------------------------------- |
| `columnsPerRow` | number of columns rendered per each row in a grid |
| `columns`       | list of _CDB_ descriptions of column items        |
| `rows`          | list of _CDB_ descriptions of grid row items      |

:::tip Additional Docs

- [Semantic-UI Grid](https://semantic-ui.com/collections/grid.html#/definition)
- [Semantic-UI React Grid](https://react.semantic-ui.com/collections/grid/)

:::

## Data

:::info Data rendering

Doesn't directly render any data referenced by the `data` path.

:::

## Example

### Container grid with a fixed number of columns

<Tabs>
  <TabItem value="layout" label="Layout" default>

```json
{[
  {
    "component": "grid",
    "columnsPerRow": 2,
    "className": "demo",
    "columns": [
      {
        "component": "placeholder",
      },
      {
        "component": "placeholder",
      },
      {
        "component": "placeholder",
      },
    ],
  },
]}
```

  <div className="playgroundHeader_node_modules-@docusaurus-theme-live-codeblock-lib-theme-Playground-styles-module">
    Result
  </div>
  <div className="playgroundPreview_node_modules-@docusaurus-theme-live-codeblock-lib-theme-Playground-styles-module">
    <GeneratedUI
      layout={[
        {
          component: 'grid',
          columnsPerRow: 2,
          className: 'demo',
          columns: [
            {
              component: 'placeholder',
            },
            {
              component: 'placeholder',
            },
            {
              component: 'placeholder',
            },
          ],
        },
      ]}
    />
  </div>
</TabItem>
<TabItem value="react" label="React" default>

```jsx live
<GeneratedUI
  layout={[
    {
      component: 'grid',
      columnsPerRow: 2,
      className: 'demo',
      columns: [
        {
          component: 'placeholder',
        },
        {
          component: 'placeholder',
        },
        {
          component: 'placeholder',
        },
      ],
    },
  ]}
/>
```

</TabItem>
<TabItem value="jinja" label="Jinja2" default>

```django
<div>
  {{
    oarepo_ui.render_layout(
      layout=[{
      component: 'grid',
      columnsPerRow: 2,
      columns: [
        {
          component: 'placeholder',
        },
        {
          component: 'placeholder',
        },
        {
          component: 'placeholder',
        },
      ],
    }])
  }}
</div>
```

    <div className="playgroundHeader_node_modules-@docusaurus-theme-live-codeblock-lib-theme-Playground-styles-module">
      Result
    </div>
    <div className="playgroundPreview_node_modules-@docusaurus-theme-live-codeblock-lib-theme-Playground-styles-module">
      <GeneratedUI
        layout={[
          {
            component: 'grid',
            columnsPerRow: 2,
            className: 'demo',
            columns: [
              {
                component: 'placeholder',
              },
              {
                component: 'placeholder',
              },
              {
                component: 'placeholder',
              },
            ],
          },
        ]}
      />
    </div>

  </TabItem>
</Tabs>

### Grid with rows

<Tabs>
  <TabItem value="react" label="React" default>

```jsx live
<GeneratedUI
  layout={[
    {
      component: 'grid',
      rows: [
        {
          columns: [
            {
              component: 'placeholder',
            },
            {
              component: 'placeholder',
            },
          ],
        },
        {
          columns: [
            {
              className: 'ten wide',
              items: [
                {
                  component: 'placeholder',
                },
              ],
            },
            {
              component: 'placeholder',
            },
          ],
        },
      ],
    },
  ]}
/>
```

</TabItem>
<TabItem value="jinja" label="Jinja2" default>

```django
<div>
  {{
    oarepo_ui.render_layout(
      layout={[{
        component: 'grid',
        rows: [
          {
            columns: [
              {
                component: 'placeholder',
              },
              {
                component: 'placeholder',
              },
            ],
          },
          {
            columns: [
              {
                className: 'ten wide',
                items: [
                  {
                    component: 'placeholder',
                  },
                ],
              },
              {
                component: 'placeholder',
              },
            ],
          },
        ],
      },
    ]})
  }}
</div>
```

</TabItem>
</Tabs>
