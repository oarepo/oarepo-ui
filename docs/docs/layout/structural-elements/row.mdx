---
sidebar_position: 1
---

import ExampleTabs from '@site/src/components/ExampleTabs'
import 'semantic-ui-css/semantic.min.css'

# Row

A layout element that places its items in a flexbox row.
Items can be separated by an optional separator.

```yaml
component: 'list'
```

:::note Implementation state:

- **Jinja2**: 🏗
- **React**: [✅](https://github.com/oarepo/react-generative-ui/blob/master/src/components/Row.tsx)

:::

## Properties

<!-- TODO: describe any properties accepted by an element -->

| Name        | Description                                                |
| ----------- | ---------------------------------------------------------- |
| `items`     | list of _CDB_ items to be rendered in a row                |
| `separator` | either a string or a _CDB_ used to separate rendered items |
| `*`         | any other properties passed to the flexbox row             |

:::tip Additional Docs

- [Semantic-UI Grid Row](https://semantic-ui.com/collections/grid.html#grouping)
- [Semantic-UI React Grid.Row](https://react.semantic-ui.com/collections/grid/)
- [CSS Flexbox](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Flexible_Box_Layout/Basic_Concepts_of_Flexbox)

:::

## Data

<!-- TODO: does element render any data given by `data` path? -->

:::info Data rendering

Doesn't directly render any data referenced by the `dataField`.

:::

## Example

### Simple row items

<ExampleTabs
  layout={[
    {
      component: 'row',
      items: [
        { component: 'label', basic: true, content: 'this is item #1' },
        { component: 'label', basic: true, content: 'this is item #2' },
        { component: 'label', basic: true, content: 'this is item #3' },
      ],
    },
  ]}
/>

### String-separated items

<ExampleTabs
  layout={[
    {
      component: 'row',
      separator: ' || ',
      items: [
        { component: 'label', basic: true, content: 'this is item #1' },
        { component: 'label', basic: true, content: 'this is item #2' },
        { component: 'label', basic: true, content: 'this is item #3' },
      ],
    },
  ]}
/>

### Component-separated items

<ExampleTabs
  layout={[
    {
      component: 'row',
      separator: {
        component: 'icon',
        name: 'arrow right',
      },
      items: [
        { component: 'label', basic: true, content: 'this is item #1' },
        { component: 'label', basic: true, content: 'this is item #2' },
        { component: 'label', basic: true, content: 'this is item #3' },
      ],
    },
  ]}
/>
