---
sidebar_position: 1
---

import ExampleTabs from '@site/src/components/ExampleTabs'
import 'semantic-ui-css/semantic.min.css'

# Authority identifier

An element that renders an identifier badge of person or organization.

```yaml
component: 'authority-identifier'
```

:::note Implementation state:

- **Jinja2**: 🏗
- **React**: [✅](https://github.com/oarepo/react-generative-ui/blob/master/src/components/record/AuthorityIdentifier.tsx)

:::

## Properties

| Name         | Description              |
| ------------ | ------------------------ |
| `identifier` | identifier value         |
| `scheme`     | scheme of the identifier |

## Data

Uses any data referenced by the `dataField` as authority identifier props:

```jsx
const {identifier, scheme} = resolvedData

<AuthorityIdentifier identifier={identifier} scheme={scheme}
```

## Example

<ExampleTabs
  layout={[
    {
      component: 'authority-identifier',
      identifier: '1234',
      scheme: 'orcid',
    },
  ]}
/>
