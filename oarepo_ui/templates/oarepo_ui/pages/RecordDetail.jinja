{#def
  record,
  record_ui,
  files,
  media_files,
  permissions,
  is_preview,
  include_deleted,
  is_draft,
  model,
  model_name,
  community,
  community_ui,
  user_avatar,
  record_owner_id,
  ui_links,
  extra_context,
  d,
#}
{% extends "invenio_app_rdm/records/detail.html" %}
{# Available template blocks:

This template defines the following blocks that can be overridden:

{%- block page_body %}  # Main page wrapper (from invenio_app_rdm/records/detail.html)
  Override the entire page body if needed.

  {%- block page_banners %}  # Banner area - includes model-specific record_detail/banners.html
  Override to provide custom banners or omit to use model-specific partial.

  {%- block page_main %}  # Main content area - includes model-specific record_detail/main.html
  Override to provide custom main content or omit to use model-specific partial.

{%- endblock page_body %}

{%- block javascript %}  # Adds record management and sharing JavaScript
  Override to customize JS includes.
{%- endblock javascript %}

Note: All blocks support {{ super() }} to preserve parent content.
The "embedded" context variable controls certain features.
#}
{%- block page_body %}
{%- block page_banners -%}
  {% include model_name ~ "/record_detail/banners.html" %}
{%- endblock page_banners -%}

{%- block page_main -%}
  {% include model_name ~ "/record_detail/main.html" %}
{%- endblock page_main -%}
{%- endblock page_body -%}

{%- block javascript %}
  {{ super() }}
  {{ webpack['record_management.js'] }}
  {% if not embedded %}
    {{ webpack["record_sharing.js"] }}
  {% endif %}
{%- endblock javascript %}
