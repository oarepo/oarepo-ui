.ui.input {
  &.icon > i.icon.clear-button {
    position: absolute;
    right: @iconMargin + 0.2em;
  }

  & > textarea+i.icon.clear-button {
    top: 50%;
    transform: translateY(-50%);
    z-index: 100;
  }
}